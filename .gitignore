# Solo se publica la app web (HTML único + PWA). Lo demás es local.
swimtracker/
*.zip
*backup*.json
*restaurado*.json
__pycache__/

PROYECTO.md
*.pdf

# Material de desarrollo de la cámara (prototipo, copia de referencia, doc de continuidad).
# La página real que SÍ se publica es camara.html en la raíz.
_camara-wip/
